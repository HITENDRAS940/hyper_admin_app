export const formatDateToYYYYMMDD = (date: Date) =>
  date.toISOString().split('T')[0];
export const calculateRevenueData = () => ({ total: 0 });
export const mapSlotsWithBookingInfo = (slots: any, bookings: any) => slots;
